{
  "name": "pic-check-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.js",
    "check": "node --check app.js && node --check legal-docs.js && node --check api/analyze.js && node --check api/fetch-image.js && node --check api/pic.js && node --check dev-server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
